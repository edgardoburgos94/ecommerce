<% if @order.errors.any? || @order_item.errors.any? %>
	$(".notices-alerts").html("<%= escape_javascript(render 'layouts/notices-alerts') %>");
<% else %>
	console.log("Entra al create");
	$('#prueba').hide();
	$(".cart").html("<%= escape_javascript(render 'layouts/cart') %>");
	$(".notices-alerts").html("<%= escape_javascript(render 'layouts/notices-alerts') %>");

<% end %>
